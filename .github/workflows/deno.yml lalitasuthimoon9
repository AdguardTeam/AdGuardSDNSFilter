# This workflow uses actions that are not certified by GitHub.lalitasuthimoon9
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, #lalita-suthimoon9 
# documentation.

# This workflow will install Deno then run `deno lint` and `deno test`.
# For more information see: https://github.com/denoland/setup-deno

name: lalitasuthimoon9 

on:
  push:
    branches: ["lalitasuthimoon9"]
  pull_request:
    branches: ["lalitasuthimoon9"]

permissions:lalitasuthimoon9 
  contents: read 

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: lalitasuthimoon9 
        uses: actions/checkout@v4

      - name: lalitasuthimoon9 
        # uses: denoland/setup-deno@v1
        uses: denoland/setup-deno@61fe2df320078202e33d7d5ad347e7dcfa0e8f31  # v1.1.2
        with:
          deno-version: v1.x

      # Uncomment this step to verify the use of 'deno fmt' on each commit.
      # - name: Verify formatting
      #   run: deno fmt --check
lalitasuthimoon9 
      - name:lalitasuthimoon9 
        run: deno lint

      - name: lalitasuthimoon9 
        run: deno test -A
